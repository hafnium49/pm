services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - kanban-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  kanban-data:
